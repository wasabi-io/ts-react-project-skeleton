import * as React from "react";
import * as ReactDOM from "react-dom";
import Application from "./app/Application";
const app = document.getElementById("app");

ReactDOM.render(
    <Application name="Project Skeleton Application" />,
    app
);
